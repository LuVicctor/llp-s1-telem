#include <stdio.h>

int main(){
  int prova1, prova2, prova3;
  float media; 

  printf("Entre com a nota1: ");
  scanf("%d", &prova1);

  scanf("%d", &prova2);

   printf("Entre com a nota3: ");
  scanf("%d", &prova3);

  media = (prova1 + prova2 + 2*prova3)/4; 

  if (media >= 60){
    printf("Aprovado com media: %2.f", media);
  }
  else{
    printf("Reprovado com media: %2.f", media);
  }
return 0;
}